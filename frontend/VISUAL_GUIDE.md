# 🎨 Visual Architecture Guide

## 📊 System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                   FUEL EU COMPLIANCE DASHBOARD               │
│                     (React + TypeScript)                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │         HEXAGONAL ARCHITECTURE          │
        └─────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
   ┌────────┐          ┌──────────┐          ┌──────────┐
   │   UI   │◄────────►│   CORE   │◄────────►│   API    │
   │ LAYER  │  Ports   │  LAYER   │  Ports   │  LAYER   │
   └────────┘          └──────────┘          └──────────┘
```

## 🏗️ Detailed Layer Structure

### 1️⃣ UI LAYER (React Components)
```
┌─────────────────────────────────────────┐
│            App.tsx                      │
│  ┌───────────────────────────────┐     │
│  │  Tab Navigation (4 tabs)      │     │
│  └───────────────────────────────┘     │
│                                         │
│  ┌──────────┬──────────┬──────────┬───┐│
│  │ Routes   │ Compare  │ Banking  │...││
│  │   Tab    │   Tab    │   Tab    │   ││
│  └──────────┴──────────┴──────────┴───┘│
│                                         │
│  Shared Components:                     │
│  • Button  • Card  • Select             │
│  • Alert   • LoadingSpinner             │
└─────────────────────────────────────────┘
```

### 2️⃣ CORE LAYER (Business Logic)
```
┌─────────────────────────────────────────┐
│          DOMAIN ENTITIES                │
│  • Route                                │
│  • Comparison                           │
│  • Banking                              │
│  • Pooling                              │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│          USE CASES                      │
│  • RoutesUseCase                        │
│  • ComparisonUseCase                    │
│  • BankingUseCase                       │
│  • PoolingUseCase                       │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│          PORTS (Interfaces)             │
│  Inbound  ◄──► Core ◄──► Outbound      │
└─────────────────────────────────────────┘
```

### 3️⃣ INFRASTRUCTURE LAYER (API)
```
┌─────────────────────────────────────────┐
│          API CLIENT                     │
│  • ApiClient (Base HTTP)                │
│  • Error Handling                       │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│         REPOSITORIES                    │
│  • RoutesApiRepository                  │
│  • BankingApiRepository                 │
│  • PoolingApiRepository                 │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│      BACKEND API (Port 3000)            │
│  /routes  /banking  /pools  /compliance │
└─────────────────────────────────────────┘
```

## 🔄 Data Flow Diagram

### Example: Setting a Baseline Route

```
USER ACTION: Click "Set Baseline"
       │
       ▼
┌──────────────────────┐
│   RoutesTab.tsx      │ ◄─── UI Component
│   (React Component)  │
└──────────────────────┘
       │
       │ calls
       ▼
┌──────────────────────┐
│  RoutesUseCase       │ ◄─── Business Logic
│  .setBaseline()      │
└──────────────────────┘
       │
       │ calls
       ▼
┌──────────────────────┐
│ RoutesApiRepository  │ ◄─── Infrastructure
│  .setBaseline()      │
└──────────────────────┘
       │
       │ HTTP POST
       ▼
┌──────────────────────┐
│  Backend API         │ ◄─── External System
│  POST /routes/:id/   │
│       baseline       │
└──────────────────────┘
       │
       │ Response
       ▼
   [Flow reverses]
       │
       ▼
   UI Updates with success message
```

## 📱 Tab Structure

### Routes Tab Flow
```
┌─────────────────────────────────────┐
│         ROUTES TAB                  │
├─────────────────────────────────────┤
│  ┌───────────────────────────────┐  │
│  │  Filters (3 dropdowns)        │  │
│  │  • Vessel Type                │  │
│  │  • Fuel Type                  │  │
│  │  │  • Year                    │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  Routes Table                 │  │
│  │  ┌──┬──┬──┬──┬──┬──┬──┬────┐  │  │
│  │  │ID│VT│FT│Yr│GH│FC│Ds│Btn │  │  │
│  │  ├──┼──┼──┼──┼──┼──┼──┼────┤  │  │
│  │  │..│..│..│..│..│..│..│Set │  │  │
│  │  └──┴──┴──┴──┴──┴──┴──┴────┘  │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

### Compare Tab Flow
```
┌─────────────────────────────────────┐
│        COMPARE TAB                  │
├─────────────────────────────────────┤
│  ┌───────────────────────────────┐  │
│  │  Target Info Box              │  │
│  │  89.3368 gCO₂e/MJ            │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  Comparison Table             │  │
│  │  Baseline vs Current          │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───┬───────────┬───────────────┐  │
│  │ % │Compliance │  vs Target    │  │
│  │Diff│  Status   │   Status     │  │
│  └───┴───────────┴───────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  Visual Progress Bars         │  │
│  │  ████████░░░░ Baseline        │  │
│  │  ██████░░░░░░ Current         │  │
│  │  ███████░░░░░ Target          │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

### Banking Tab Flow
```
┌─────────────────────────────────────┐
│        BANKING TAB (Article 20)     │
├─────────────────────────────────────┤
│  ┌───────────────────────────────┐  │
│  │  Year Selector                │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────┬───────┬───────────────┐  │
│  │ Year  │  CB   │  Status       │  │
│  │ 2024  │+150.5 │ ✅ Can Bank   │  │
│  └───────┴───────┴───────────────┘  │
│                                     │
│  ┌────────────────┬──────────────┐  │
│  │  Bank Surplus  │ Apply Banked │  │
│  │                │              │  │
│  │  [Amount: __]  │ [Year: ___]  │  │
│  │  [Bank Button] │[Amount: __]  │  │
│  │                │[Apply Button]│  │
│  └────────────────┴──────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  Operation Result             │  │
│  │  Before: 150.5                │  │
│  │  Applied: 50.0                │  │
│  │  After: 100.5                 │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

### Pooling Tab Flow
```
┌─────────────────────────────────────┐
│       POOLING TAB (Article 21)      │
├─────────────────────────────────────┤
│  ┌───────────────────────────────┐  │
│  │  Year Selector                │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  Vessel Selection Table       │  │
│  │  [☑] Vessel-001  CB: +50.0   │  │
│  │  [☑] Vessel-002  CB: -30.0   │  │
│  │  [ ] Vessel-003  CB: +20.0   │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  Pool Configuration           │  │
│  │  Total CB: +20.0 ✅          │  │
│  │  Selected: 2 vessels          │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌──────────────┬───────────────┐  │
│  │  Validate    │ Create Pool   │  │
│  │   Pool       │   (disabled)  │  │
│  └──────────────┴───────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  Validation Results           │  │
│  │  ✅ Pool sum >= 0            │  │
│  │  ✅ Deficit protected        │  │
│  │  ✅ Surplus protected        │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

## 🎯 Component Hierarchy

```
App
├── Header
├── TabNavigation
│   ├── RoutesTab
│   │   ├── Card (Filters)
│   │   │   └── Select × 3
│   │   └── Card (Table)
│   │       ├── LoadingSpinner
│   │       ├── Alert
│   │       └── Table
│   │           └── Button (Set Baseline)
│   │
│   ├── CompareTab
│   │   ├── Card (Target Info)
│   │   ├── Card (Comparison Table)
│   │   ├── Card (Performance KPIs)
│   │   └── Card (Visual Bars)
│   │
│   ├── BankingTab
│   │   ├── Card (Info)
│   │   │   └── Select (Year)
│   │   ├── Card (Current CB)
│   │   ├── Card (Operations)
│   │   │   ├── Bank Panel
│   │   │   │   ├── Input
│   │   │   │   └── Button
│   │   │   └── Apply Panel
│   │   │       ├── Select
│   │   │       ├── Input
│   │   │       └── Button
│   │   └── Card (Results)
│   │
│   └── PoolingTab
│       ├── Card (Info)
│       │   └── Select (Year)
│       ├── Card (Vessel Table)
│       │   └── Checkbox × N
│       ├── Card (Pool Config)
│       │   ├── Summary
│       │   ├── Button (Validate)
│       │   └── Button (Create)
│       └── Card (Validation)
│           └── Results Table
│
└── Footer
```

## 📊 State Management

### Component State Flow
```
┌──────────────────────┐
│   User Interaction   │
│   (Click, Input)     │
└──────────────────────┘
          │
          ▼
┌──────────────────────┐
│   useState Hook      │
│   (Local State)      │
└──────────────────────┘
          │
          ▼
┌──────────────────────┐
│   useAsync Hook      │
│   (API Call)         │
└──────────────────────┘
          │
          ▼
┌──────────────────────┐
│   Use Case           │
│   (Business Logic)   │
└──────────────────────┘
          │
          ▼
┌──────────────────────┐
│   Repository         │
│   (API Client)       │
└──────────────────────┘
          │
          ▼
┌──────────────────────┐
│   Backend API        │
└──────────────────────┘
          │
          ▼
   [Response flows back]
          │
          ▼
┌──────────────────────┐
│   Component          │
│   Re-renders         │
└──────────────────────┘
```

## 🔐 Type Safety Flow

```
TypeScript Types (Domain)
          │
          ▼
Interface Definitions (Ports)
          │
          ▼
Use Case Implementation
          │
          ▼
Repository Implementation
          │
          ▼
API Response Validation
          │
          ▼
Component Props
          │
          ▼
UI Rendering
```

## 🎨 Styling Architecture

```
Tailwind CSS
     │
     ├── Utility Classes
     │   └── Applied directly in JSX
     │
     ├── Custom Components
     │   ├── Button (variants)
     │   ├── Card
     │   ├── Select
     │   └── Alert
     │
     └── Responsive Design
         ├── Mobile (default)
         ├── Tablet (md:)
         └── Desktop (lg:)
```

## 📈 Performance Optimization

```
Code Splitting
     │
     ├── Lazy Loading (future)
     │   └── Dynamic imports
     │
     ├── Memoization (future)
     │   ├── React.memo()
     │   └── useMemo()
     │
     └── Async Data Loading
         └── useAsync hook
```

## 🧩 Dependency Injection Pattern

```
main.tsx (Entry Point)
     │
     ├── Create ApiClient
     │
     ├── Create Repositories
     │   ├── RoutesApiRepository(apiClient)
     │   ├── BankingApiRepository(apiClient)
     │   └── PoolingApiRepository(apiClient)
     │
     ├── Create Use Cases
     │   ├── RoutesUseCase(routesRepo)
     │   ├── ComparisonUseCase(routesRepo)
     │   ├── BankingUseCase(bankingRepo)
     │   └── PoolingUseCase(poolingRepo)
     │
     └── Inject into App
         └── App(useCases)
             └── Pass to Tab Components
```

## 🎯 Key Design Patterns

1. **Hexagonal Architecture** - Clean separation
2. **Repository Pattern** - Data access abstraction
3. **Dependency Injection** - Loose coupling
4. **Use Case Pattern** - Business logic isolation
5. **Adapter Pattern** - External system integration
6. **Composite Pattern** - Component composition

---

## 🚀 Quick Reference

### File Locations
- **Domain**: `src/core/domain/`
- **Use Cases**: `src/core/application/`
- **Ports**: `src/core/ports/`
- **UI**: `src/adapters/ui/`
- **API**: `src/adapters/infrastructure/api/`
- **Shared**: `src/shared/`

### Key Concepts
- **Inbound Port**: UI → Core interface
- **Outbound Port**: Core → Infrastructure interface
- **Use Case**: Business logic implementation
- **Repository**: Data access implementation
- **Entity**: Domain model

---

This visual guide helps you understand the architecture at a glance! 🎨
